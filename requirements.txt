customtkinter>=5.2.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
scipy>=1.10.0
python-tsp>=0.2.0
tqdm>=4.65.0
Pillow>=10.0.0
psutil>=5.9.0

# Platform-specific dependencies (optional)
# Windows: pywin32 (for some Windows-specific features)
# Linux: python3-xlib (for Linux-specific features)
